# API Configuration
VITE_API_BASE_URL=http://localhost:4000
VITE_API_TIMEOUT_MS=15000

# Data Source Mode
# true  → Demo mode: use mock data, no backend calls
# false → Real mode: use REST API, no mock data
VITE_USE_MOCK_DATA=true
